# Leave blank in development: the Vite dev server proxies /api to the backend
# (see vite.config.js). Set this only when the frontend is served from a
# different origin than the API, e.g. VITE_API_BASE=https://api.example.com
VITE_API_BASE=
